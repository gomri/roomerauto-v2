from Vars import TEST_TO_RUN

def room_type_handler(url, string, dic_to_add_url): # the 3rd var is the name of the dict that you want to add the room url to 
	if string == 'T':
		pass
	elif string == 'E':
		pass
	elif string == 'H':
		pass
	else:
		pass

must have
	13	improve error handling function
	7	handle reservation already bougth status
		added to report price per night, rate plan
		fix choose your destination test (something wrong with picking the rooms from the list)

would be nice	
	create config file 
	check if reservation is smart?
	insert the error handler to each function in the buy flow
	write reporter class (should be very easy)
